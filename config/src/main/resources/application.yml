server:
  port: 9443

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/warehouse_db
    username: admin
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
